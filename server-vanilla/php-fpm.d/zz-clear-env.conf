; php-fpm clears worker environment variables by default, which would hide the
; DB_* vars passed via docker env_file from getenv(). Keep them so the app can
; read its configuration from the container environment.
[www]
clear_env = no
