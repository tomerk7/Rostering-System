# Copy to .env and fill in. .env is gitignored — never commit real secrets.
DB_HOST=db
DB_PORT=5432
DB_DATABASE=rostering
DB_USERNAME=rostering
DB_PASSWORD=

# Secret for signing JWTs. Generate one with: php -r 'echo bin2hex(random_bytes(32));'
JWT_SECRET=
JWT_TTL_SECONDS=3600
