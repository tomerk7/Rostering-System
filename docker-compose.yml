services:
  server:
    build:
      context: ./server
    container_name: rostering_server
    env_file:
      - ./server/.env
    ports:
      - "8000:8000"
    volumes:
      - ./server:/var/www
      - server_vendor:/var/www/vendor

volumes:
  server_vendor:
